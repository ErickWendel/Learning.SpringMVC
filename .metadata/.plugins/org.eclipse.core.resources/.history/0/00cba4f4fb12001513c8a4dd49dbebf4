package com.callsystem.dao.base;

import java.util.List;

public interface IBaseDAO<T> {
	List<T> listar(String query);
	void cadastrar(T item);
	void deletar(T item);
	void atualizar(T item);
	T pesquisarPorId(String query, int id);
}
